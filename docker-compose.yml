services:
  postgres:
    image: postgres:16
    container_name: gym-tracker-db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: gymtrackerdb
    ports:
      - "5432:5432"